# ⚙️ Pet-Saúde VSA - API

API desenvolvida em Node.js para dar suporte ao aplicativo Pet-Saúde VSA.

## 🚀 Como rodar o projeto localmente

1. **Instale as dependências:**
   `npm install`

2. **Inicie o servidor:**
   `node index.js`
